/**
 * useSettings — thin re-export of the canonical feature implementation.
 *
 * This file exists for backward compatibility during FSD migration.
 * All new code should import from @/features/manage-settings/hooks/useSettings.
 */
export { useSettings } from "@/features/manage-settings/hooks/useSettings";
