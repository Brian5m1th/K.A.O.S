/**
 * useUpdaterService — thin re-export of the canonical feature implementation.
 *
 * All new code should import from @/features/auto-update/hooks/useUpdaterService.
 */
export { useUpdaterService } from "@/features/auto-update/hooks/useUpdaterService";
