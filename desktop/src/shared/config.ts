/**
 * K.A.O.S Desktop Configuration
 *
 * All API URLs should reference this single source of truth.
 * In production/Tauri builds, change this to the deployed backend URL.
 */
export const API_BASE_URL = "http://localhost:8000";
